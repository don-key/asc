<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
                        
<!-- 네임스페이스(사용영역) 설정 -->
<mapper namespace="com.object.asc.mapper.UserMapper">

<!-- 회원가입 -->
<insert id="register">

INSERT INTO users(id, password, name, phone, status) 
VALUES (#{id}, #{password}, #{name}, #{phone}, #{status})

</insert>

<!-- 회원정보 수정 -->
<update id="modify">
UPDATE users SET password = #{password}, name = #{name}, phone = #{phone}, photo = #{photo}, status = #{status} 
WHERE userNo = #{userNo};
</update>

</mapper>